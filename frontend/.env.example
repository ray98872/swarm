# Optional: override the backend URL at build time.
# If unset, the app uses http://localhost:8080 in dev and
# https://swarm-backend.fly.dev in production.
# VITE_API_BASE=https://swarm-backend.fly.dev
